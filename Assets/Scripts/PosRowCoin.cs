using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PosRowCoin : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //GameObject parentGameObject = transform.parent.gameObject;
        //bool canPos = parentGameObject.name.Contains("platformZSplit") ||
        //        parentGameObject.name.Contains("platformZThin");

        //if (!canPos)
        //{
        //    float randomPosX = Random.Range(-1.8f, 1.8f);
        //    transform.position = new Vector3(randomPosX, transform.position.y, transform.position.z);
        //}
    }
}
